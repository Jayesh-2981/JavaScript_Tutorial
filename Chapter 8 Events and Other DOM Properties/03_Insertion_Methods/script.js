let a = document.getElementsByTagName("div")[0];
// a.innerHTML = a.innerHTML + "<h1>Hello World!</h1>";   // It will create h1 tag in inner div

let div = document.createElement("div");
div.innerHTML = "<h1>Hello World</h1>";

// node.append(e)
// a.append(div);

// node.prepend(e)
// a.prepend(div);

// node.before(e)
// a.before(div);

// node.after(e)
// a.after(div);

// node.replaceWith(e)
// a.replaceWith(div);
