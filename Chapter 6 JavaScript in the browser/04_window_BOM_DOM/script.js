window.console.log(window); // window is a global object in javascript  // window represents a browser window
console.log(document.body); //executes body tag
document.body.style.background = "yellow"; // We can change css properties also // it will become inline css
